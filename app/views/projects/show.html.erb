<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @project.title %>
</p>

<p>
  <b>Content:</b>
  <%= @project.content %>
</p>

<% if current_user %>
<td><%= link_to 'Edit', edit_project_path(@project), :class => "btn btn-primary btn-mini" %></td>
<% end %>

<td><%= link_to 'Back', projects_path, :class => "btn btn-inverse btn-mini" %></td>